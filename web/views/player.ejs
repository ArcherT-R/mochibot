<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= player.username %> Profile</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f6f8;
      margin: 0;
    }
    .top-bar {
      background-color: #1E90FF;
      color: white;
      padding: 15px 20px;
      font-size: 1.5em;
    }
    .container {
      max-width: 800px;
      margin: 20px auto;
      padding: 0 20px;
    }
    .player-header {
      display: flex;
      align-items: center;
      gap: 20px;
      margin-bottom: 20px;
      background: white;
      padding: 15px;
      border-radius: 10px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }
    .player-header img {
      border-radius: 50%;
    }
    .player-header div {
      flex: 1;
    }
    .sessions {
      margin-top: 20px;
      background: white;
      padding: 15px;
      border-radius: 10px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }
    .sessions h2 {
      margin-top: 0;
    }
    .session-item {
      padding: 10px;
      border-bottom: 1px solid #eee;
      display: flex;
      justify-content: space-between;
    }
    .session-item:last-child {
      border-bottom: none;
    }
  </style>
</head>
<body>
  <div class="top-bar">Player Profile</div>
  <div class="container">
    <div class="player-header">
      <img src="https://thumbnails.roblox.com/v1/users/avatar-headshot?userIds=<%= player.roblox_id %>&size=150x150&format=Png&isCircular=true" alt="<%= player.username %>">
      <div>
        <h1><%= player.username %></h1>
        <p>Group Rank: <%= player.group_rank %></p>
        <p>Weekly Minutes: <%= player.weekly_minutes %></p>
      </div>
    </div>

    <div class="sessions">
      <h2>Sessions</h2>
      <% if (sessions && sessions.length > 0) { %>
        <% sessions.forEach(s => { %>
          <div class="session-item">
            <span><%= new Date(s.session_start).toLocaleString() %> → <%= new Date(s.session_end).toLocaleString() %></span>
            <span><%= s.minutes_played %> mins</span>
          </div>
        <% }) %>
      <% } else { %>
        <p>No sessions logged yet.</p>
      <% } %>
    </div>
  </div>
</body>
</html>
