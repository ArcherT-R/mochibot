<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Mochi Bar - <%= player.username %></title>
<style>
  /* Original CSS */
  body { font-family: Arial, sans-serif; background: #f0f0f0; margin:0; padding:0; }
  header { background: #6cb4ee; padding: 20px; text-align:center; font-size:24px; font-weight:bold; color: white; }
  .container { max-width: 1200px; margin:auto; padding:20px; display:flex; gap:20px; flex-wrap: wrap; }
  .column { background:white; padding:15px; border-radius:8px; flex:1; min-width:250px; box-sizing:border-box; }
  .column h3 { margin-top:0; border-bottom:2px solid #000; padding-bottom:5px; }
  .avatar { width:150px; height:150px; border-radius:50%; margin-bottom:10px; max-width:100%; }
  .box { background:#e9e9e9; padding:10px; border-radius:6px; margin-bottom:10px; word-wrap: break-word; }

  /* Scrollable past sessions */
  .sessions-scroll {
    max-height: 300px;
    overflow-y: auto;
    padding-right: 5px;
  }

  /* Updated: Past session card styling for clickability */
  .session-box { 
    background:#f5f5f5; 
    padding:8px; 
    border-radius:6px; 
    margin-bottom:6px; 
    word-wrap: break-word; 
    cursor: pointer; /* Indicates clickability */
    transition: background 0.2s;
  }
  .session-box:hover {
    background: #e0e0e0;
  }

  /* New: Live Session Counter Style */
  #live-time { font-size: 1.2em; font-weight: bold; color: #007bff; }

  /* New: Modal for Session Details */
  .modal {
    display: none; 
    position: fixed; 
    z-index: 10; 
    left: 0; 
    top: 0;
    width: 100%; 
    height: 100%; 
    overflow: auto; 
    background-color: rgba(0,0,0,0.4); 
  }
  .modal-content {
    background-color: #fefefe;
    margin: 10% auto; 
    padding: 0;
    border-radius: 10px;
    width: 80%; 
    max-width: 400px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.3);
    overflow: hidden;
  }
  .modal-header {
    background-color: #6cb4ee; /* Blue banner */
    color: white;
    padding: 10px;
    text-align: center;
    font-weight: bold;
    position: relative;
    /* Ensures text is visible */
    word-wrap: break-word; 
  }
  .modal-body { padding: 20px; }
  .modal-body strong { display: inline-block; width: 100px; }
  .close {
    color: white;
    float: right;
    font-size: 28px;
    font-weight: bold;
    position: absolute;
    right: 10px;
    top: 5px;
  }
  .close:hover,
  .close:focus {
    color: #ccc;
    text-decoration: none;
    cursor: pointer;
  }

  /* Responsive: stack columns on mobile (Original) */
  @media (max-width: 768px) {
    .container { flex-direction: column; gap:15px; padding:15px; }
    .column { width: 100%; }
    .avatar { width:100%; height:auto; }
    .modal-content { width: 90%; margin: 20% auto; }
  }
</style>
</head>
<body>

<header>Mochi Bar - <%= player.username %></header>

<div class="container">

    <div class="column">
    <h3>Profile</h3>
    <img src="<%= player.avatar_url %>" alt="Avatar" class="avatar" onerror="this.src='https://placehold.co/150x150/e0e6f0/5f6c7b?text=Error';">
    <div class="box"><strong>Username:</strong> <%= player.username %></div>
    <div class="box"><strong>Group Rank:</strong> <%= player.group_rank %></div>
    <div class="box"><strong>Notes:</strong> <%= player.notes || 'No notes available.' %></div>
  </div>

    <div class="column">
    <h3>Sessions</h3>

        <% if (ongoingSession) { 
      const startTime = new Date(ongoingSession.start || Date.now()).getTime(); 
    %>
      <div class="box" id="ongoing-session-box">
        <strong>🟢 ONGOING SESSION</strong><br>
        <strong>Started:</strong> <%= new Date(startTime).toLocaleString() %><br>
        <strong>Time in Game:</strong> <span id="live-time">00:00:00</span>
        <input type="hidden" id="session-start-time" value="<%= startTime %>">
      </div>
    <% } else { %>
      <div class="box">No ongoing session</div>
    <% } %>

    <hr>

        <div class="sessions-scroll">
      <% if (sessions && sessions.length > 0) { %>
        <% sessions.slice(0,4).forEach((s, index) => { %>
          <div class="session-box" onclick="showSessionDetails(<%= index %>)">
            <strong>Session <%= index + 1 %></strong><br>
            <span>Minutes: <%= s.minutes_played %></span>
          </div>
        <% }) %>
      <% } else { %>
        <div class="session-box">No past sessions.</div>
      <% } %>
    </div>
  </div>

    <div class="column">
    <h3>Shifts</h3>
    <div class="box"><strong>Attended:</strong> <%= shifts.attended || 0 %></div>
    
        <div class="box">
        <strong>Hosted/Co-Hosted:</strong> 
        <%= (shifts.hosted || 0) + (shifts.coHosted ? shifts.coHosted.length : 0) %>
    </div>

    <% if (shifts.coHosted && shifts.coHosted.length > 0) { %>
      <hr>
      <h4>Co-Hosted Details</h4>
      <% shifts.coHosted.forEach(c => { %>
        <div class="box">
          <strong>Event:</strong> <%= c.name %><br>
          <span>Host: <%= c.host %></span>
        </div>
      <% }) %>
    <% } %>
  </div>

</div>

<div id="sessionModal" class="modal">
  <div class="modal-content">
    <div class="modal-header">
      <span class="close" onclick="closeModal()">&times;</span>
            <span id="modal-header-text"><%= player.username %> - <%= player.group_rank %></span>
    </div>
    <div class="modal-body">
            <p><strong>Username:</strong> <%= player.username %></p>
      <p><strong>Time In Game:</strong> <span id="modal-minutes-played"></span> minutes</p>
      <p><strong>Session Start:</strong> <span id="modal-session-start"></span></p>
      <p><strong>Session End:</strong> <span id="modal-session-end"></span></p>
    </div>
  </div>
</div>

<script>
    // Data passed from server (full sessions list)
    const allSessions = JSON.parse('<%- JSON.stringify(sessions || []) %>');
    const playerUsername = "<%= player.username %>";
    const playerRank = "<%= player.group_rank %>";
    const modal = document.getElementById('sessionModal');

    // Utility function to format milliseconds to HH:MM:SS
    function formatTime(ms) {
        let totalSeconds = Math.floor(ms / 1000);
        let hours = Math.floor(totalSeconds / 3600);
        let minutes = Math.floor((totalSeconds % 3600) / 60);
        let seconds = totalSeconds % 60;

        const pad = num => String(num).padStart(2, '0');
        return `${pad(hours)}:${pad(minutes)}:${pad(seconds)}`;
    }

    /* ----------------------------------
     * 1. Live Counter Logic 
     * ---------------------------------- */

    const startTimeInput = document.getElementById('session-start-time');
    if (startTimeInput) {
        const startTimeMs = parseInt(startTimeInput.value);
        
        function updateLiveCounter() {
            const elapsed = Date.now() - startTimeMs;
            document.getElementById('live-time').textContent = formatTime(elapsed);
        }

        updateLiveCounter();
        setInterval(updateLiveCounter, 1000);
    }


    /* ----------------------------------
     * 2. Modal/Clickable Session Logic
     * ---------------------------------- */

    function showSessionDetails(index) {
        const session = allSessions[index];
        if (!session) return;

        // Set Header (Blue banner content)
        const headerText = `${playerUsername} - ${playerRank}`;
        document.getElementById('modal-header-text').textContent = headerText;

        // Set Body Content
        document.getElementById('modal-minutes-played').textContent = session.minutes_played;
        document.getElementById('modal-session-start').textContent = new Date(session.session_start).toLocaleString();
        document.getElementById('modal-session-end').textContent = new Date(session.session_end).toLocaleString();

        // Show Modal
        modal.style.display = "block";
    }

    function closeModal() {
        modal.style.display = "none";
    }

    // Close the modal if the user clicks anywhere outside of the modal content
    window.onclick = function(event) {
        if (event.target == modal) {
            closeModal();
        }
    }
</script>

</body>
</html>
