<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= player.username %></title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
      margin: 0;
      background: #f0f2f5;
    }
    header {
      background: #1e40af;
      color: white;
      padding: 1rem 2rem;
      font-size: 1.5rem;
      font-weight: 600;
    }
    .container {
      padding: 2rem;
      max-width: 800px;
      margin: auto;
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
    }
    .card {
      background: white;
      padding: 1rem;
      border-radius: 0.75rem;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }
    .player-header {
      display: flex;
      align-items: center;
      gap: 1rem;
    }
    .player-header img {
      border-radius: 50%;
      width: 100px;
      height: 100px;
    }
    h2 {
      margin-top: 0;
    }
    ul {
      padding-left: 1.2rem;
    }
    li {
      margin-bottom: 0.5rem;
    }
  </style>
</head>
<body>
  <header><%= player.username %> Profile</header>
  <div class="container">
    <div class="card player-header">
      <img src="https://tr.rbxcdn.com/<%= player.roblox_id %>/150/150/AvatarHeadshot.png" alt="<%= player.username %>">
      <div>
        <div><strong>Username:</strong> <%= player.username %></div>
        <div><strong>Group Rank:</strong> <%= player.group_rank %></div>
        <div><strong>Weekly Minutes:</strong> <%= player.weekly_minutes || 0 %></div>
      </div>
    </div>

    <div class="card">
      <h2>Play Sessions</h2>
      <ul>
        <% if (sessions && sessions.length > 0) { %>
          <% sessions.forEach(s => { %>
            <li>
              <strong>Start:</strong> <%= new Date(s.session_start).toLocaleString() %> <br>
              <strong>End:</strong> <%= new Date(s.session_end).toLocaleString() %> <br>
              <strong>Duration:</strong> <%= s.minutes_played %> minute<%= s.minutes_played !== 1 ? 's' : '' %>
            </li>
          <% }) %>
        <% } else { %>
          <li>No sessions yet.</li>
        <% } %>
      </ul>
    </div>
  </div>
</body>
</html>
